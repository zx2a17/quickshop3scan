import 'package:flutter/material.dart';

class ListShoppingPage extends StatelessWidget {
  const ListShoppingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
